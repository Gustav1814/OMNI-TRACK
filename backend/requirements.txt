# ============================================================
# OmniTrack AI — Python Dependencies
# ============================================================
# Requires Python 3.11 or 3.12 (TensorFlow, needed by DeepFace, has no
# wheel for 3.14 — emotion recognition silently falls back to mock mode there).

# --- Core Framework ---
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.10.0
pydantic-settings>=2.7.0
python-multipart>=0.0.20

# --- Database ---
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29.0
pgvector>=0.3.0
alembic>=1.14.0
greenlet>=3.0.0

# --- Auth & Security ---
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
# passlib 1.7.x expects bcrypt.__about__; removed in bcrypt 4.1+
bcrypt>=4.0.1,<4.1
pycryptodome>=3.21.0

# --- AI / Computer Vision ---
ultralytics>=8.3.0
torch>=2.1.0
torchvision>=0.16.0
torchreid>=0.2.5
# torchreid imports tensorboard unconditionally; without it Re-ID falls back to mock.
tensorboard>=2.16
# <5: OpenCV 5 dropped the bundled haarcascade data and the dnn module wheels
# DeepFace's opencv/ssd detectors load, so face detection fails at import.
opencv-python-headless>=4.10.0,<5
numpy>=1.24.0
scipy>=1.11.0
Pillow>=10.0.0

# Emotion recognition (DeepFace/FER)
deepface>=0.0.90
# TF 2.16+ split Keras out; DeepFace/retinaface still need the legacy API.
# Without this the import fails at load time and emotion silently mocks.
tf-keras>=2.16

# --- Real-time & Caching ---
redis>=5.2.0
websockets>=14.0

# --- Utilities ---
python-dotenv>=1.0.0
loguru>=0.7.0
psutil>=5.9.8

# ============================================================
# Optional extras — install manually only if you use these paths
# ============================================================
# mediapipe>=0.10.14         # pose estimation (app/ai/pose_mediapipe.py)
# faiss-cpu>=1.8.0           # alternative vector store (app/services/vector_store.py)
# qdrant-client>=1.10.0      # alternative vector store (app/services/vector_store.py)
# aiokafka>=0.11.0           # Kafka event bus backend (app/services/event_bus.py)
# FastReID: alternative Re-ID backend (app/ai/reid_backend.py) — install from
# source, platform/GPU-specific: https://github.com/JDAI-CV/fast-reid
