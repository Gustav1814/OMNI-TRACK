# ============================================================
# OmniTrack AI — Python Dependencies (Proposal-compliant)
# ============================================================
# Full project standard: Emotion recognition uses DeepFace/FER (proposal).
# For full install use Python 3.11 or 3.12. TensorFlow has no wheel for 3.14.
# See README "Project standard (dependencies)" and requirements-py314.txt if using 3.14.

-r requirements-core.txt

deepface>=0.0.90
# TF 2.16+ split Keras out; DeepFace/retinaface still need the legacy API.
# Without this the import fails at load time and emotion silently mocks.
tf-keras>=2.16
